apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vless-server
spec:
  secretName: vless-server-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - vless.outi.ir
