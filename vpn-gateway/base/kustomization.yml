apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yml
  - service.yml
  - configmap.yml

configMapGenerator:
  - name: wireguard-config
    files:
      - wg0.conf
    options:
      disableNameSuffixHash: true
