apiVersion: v1
kind: Service
metadata:
  name: mieru
  labels:
    app: mieru
spec:
  type: NodePort
  ports:
    - name: mieru-tcp
      port: 443
      targetPort: 443
      nodePort: 30444
      protocol: TCP
  selector:
    app: mieru
