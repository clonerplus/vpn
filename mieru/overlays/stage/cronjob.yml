apiVersion: batch/v1
kind: CronJob
metadata:
  name: mieru-restart
  namespace: vpn
spec:
  schedule: "0 4 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          serviceAccountName: default
          containers:
            - name: kubectl
              image: bitnami/kubectl:latest
              command:
                - /bin/sh
                - -c
                - kubectl rollout restart deployment/mieru -n vpn
          restartPolicy: Never
