apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vless-httpx-outi-crt
spec:
  secretName: vless-httpx-outi-tls
  dnsNames:
    - "vless-httpx.outi.ir"
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
