apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yml
  - service.yml
  - certificate.yml

configMapGenerator:
  - name: vless-http-config
    files:
      - config/config.json
    options:
      disableNameSuffixHash: true
